TARGET_NAME= Client
ifeq "$(XDK_TARGET_PLATFORM)" "android"
TARGET_TYPE = so
else
TARGET_TYPE= eco
endif

CAR_FLAGS += -n -u
LUBE_FLAGS += -n -u

# TODO: You need to Add your include path.
INCLUDES += $(MAKEDIR)/../Carrier

SOURCES = Client.car
SOURCES += CClient.cpp
SOURCES += $(MAKEDIR)/../Carrier/Base64.cpp
SOURCES += $(MAKEDIR)/../Carrier/Carrier.cpp

LIBRARIES = $(XDK_USER_LIB)/Elastos.Runtime.lib
LIBRARIES += -lstdc++

ifeq "$(XDK_TARGET_PLATFORM)" "android"
LIBRARIES += $(XDK_TARGETS)/libElastos.Runtime.so
LIBRARIES += $(XDK_TARGETS)/libElastos.CoreLibrary.so
else
LIBRARIES += $(XDK_TARGETS)/Elastos.Runtime.eco
LIBRARIES += $(XDK_TARGETS)/Elastos.CoreLibrary.eco
LIBRARIES += $(XDK_TARGETS)/libelacarrier.so
LIBRARIES += $(XDK_TARGETS)/libelasession.so
LIBRARIES += $(XDK_TARGETS)/libelacommon.so
LIBRARIES += -lpthread
endif


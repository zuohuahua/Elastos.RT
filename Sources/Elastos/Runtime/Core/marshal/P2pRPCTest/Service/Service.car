
module
{
    importlib("Elastos.CoreLibrary");

    interface IService {
        AddService(
            [in] String name,
            [in] IInterface *servic);

        Start();
    }

    class CService {
        constructor(
            [in] String location);
        interface IService;
    }
}
